### Go has embeded tests

To start use them just create file with `name + _test.go`. 
Inside that file import "testing" file, create unit test functions with naming convention `Test + func name(t *testing)`

```
$ go test -v 
```
